import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
        domains: ['pixabay.com','cdn.pixabay.com','images.unsplash.com','www.fffuel.co','images.pexels.com'],
        
    },
};

export default nextConfig;
